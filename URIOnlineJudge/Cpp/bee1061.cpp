#include <iostream>
#include <cstring>
#include <string>

using namespace std;

int main()
{
  string line1, line2, line3, line4;

  getline(cin, line1, '\n');
  getline(cin, line2, '\n');
  getline(cin, line3, '\n');
  getline(cin, line4, '\n');

  // extrair os chars de dentro das strings
  // converter o char para int
  // converter tudo para segundos
  // calcular diferença
  // exibir na saída padrão
  // fim :)

  return 0;
}
